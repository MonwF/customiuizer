# CustoMIUIzer A14 各版本性能与稳定性报告

> 报告日期：2026-07-24  
> 数据来源：`README.md`、`CHANGELOG.md`、历次 LSPosed/Vector 日志、APK 构建产物。  
> 结论：版本号递增 **不等于** 各项指标线性更优；每一版都要分开看“改了什么、验证了什么、有没有回退”。

---

## 1. 版本-指标总览

| 版本 | 发布状态 | APK 大小 | 相对上一版 | 模块加载中位数（LSPosed 日志） | 主要定位 | 稳定性结论 |
|---|---|---:|---:|---:|---|---|
| 上游 v24.10.12 | 参考基线 | 2,785,364 B | Android 14 基线 | — | 上游最后阶段 Android 14 参考（API 100） | 稳定，但含联网权限与支持资源 |
| r14.0.0 | 初始版 | 2,901,900 B | +116,536 B | — | 建立独立 API 101 版本线，加入类/方法/参数/资源/主题缓存 | 引入大量新代码，需完整重启验证 |
| r14.1.3 | 稳定版 | 2,886,250 B | −15,650 B | 11.5 ms（首启）/ 8 ms（再启） | API 101 稳定性修复、有界线程池、移除网络权限与支持资源 | 稳定；资源治理幅度最大 |
| r14.2.0 | 稳定版 | 2,886,165 B | −85 B | 8 ms | 偏好热路径、Hook 参数、反射、并发、秒针调度 | 长期运行热路径最完整 |
| r14.2.4 | 稳定版 | 2,886,165 B | 0 B | — | 热路径缓存、反射哨兵、生命周期、无效 Hook/Receiver 防重注册 | 稳定（累积 r14.2.1-2.3） |
| r14.2.7 | 稳定版 | 2,886,165 B | 0 B | — | 自定义动作 gate、Launcher gesture gate、ContentObserver/Handler 生命周期 | 稳定 |
| r14.2.9 | 稳定版 | 2,886,165 B | −4 B | — | StepCounter 生命周期、BatteryIndicator 绘制热路径缓存 | 稳定（含 r14.2.8 累积） |
| r14.3.1 | 稳定版 | 2,886,165 B | 0 B | 15 ms（2 个样本） | 锁屏充电去重、lint 清理、WeatherDataController 线程池化 | 稳定；功能关闭零成本 |
| r14.5.0 | 稳定版 | 2,886,373 B | +208 B | — | 包名迁移、A/B/C 静态清理、生命周期加固、`getResId()` 热路径缓存 | **当前推荐稳定版**；完整重启验证无模块崩溃/ANR |
| r14.6.0 | 候选版（已回退） | 2,900,237 B | +13,864 B | — | D 类废弃 API/权限迁移：Settings.System → SharedPreferences、PendingIntent 辅助、通知渠道/权限 | **回归**：systemui `onPackageReady` 崩溃，statusbar/control center 失效 |
| r14.6.1 | 候选版 | 2,900,233 B | −4 B | 待补充 | r14.6.0 热修复：回退 Settings.System 迁移，保留通知渠道与权限声明 | LSPosed 日志无模块报错；实机功能验证待补充 |

---

## 2. 分项趋势分析

### 2.1 APK 体积

- **r14.0.0 → r14.1.3**：体积从 2,901,900 B 降到 2,886,250 B（−15,650 B），主要收益来自**移除联网权限、支持资源、限制图标缓存**。
- **r14.1.3 → r14.2.9**：基本保持在 2,886,165 B，说明后续优化以**运行时代码路径**为主，没有引入新资源或库。
- **r14.3.1 → r14.5.0**：仅 +208 B，包名迁移带来少量字符串/清单变化，但 `getResId()` 缓存抵消了这部分增长。
- **r14.6.0**：突然增加 **+13,864 B**，原因是新增了 `NotificationChannel`、权限声明和 `Helpers` 辅助方法。其中 Safe D 类改动本身不大，但导致系统级崩溃。
- **r14.6.1**：体积几乎不变（−4 B），说明热修复只是代码回退，没有新增资源。

> **体积不是性能**：APK 增大主要来自清单/辅助方法/权限字符串；运行时内存与 CPU 开销是否增加，还要看实际加载的类与执行路径。

### 2.2 启动/加载速度

`README.md` 中记录的部分 LSPosed 加载时间：

| 版本 | 样本数 | 中位数 | 平均值 | 范围 |
|---|---|---:|---:|---:|
| r14.1.3 第一次启动 | 8 | 11.5 ms | 18.9 ms | 5–51 ms |
| r14.1.3 第二次启动 | 9 | 8 ms | 15.7 ms | 5–37 ms |
| r14.2.0 正式版 | 9 | 8 ms | 22.8 ms | 5–81 ms |
| r14.3.1 完整重启后 | 2 | 15 ms | 15 ms | 6–24 ms |

- **趋势**：中位数在 **8–15 ms** 之间波动，没有持续下降。
- **原因**：模块入口加载时间受目标进程（SystemUI、Settings 等）启动负载影响较大，单次测量不能证明“越新版本越快”。
- **结论**：后续版本通过 gate、缓存、生命周期治理**减少的是长期运行热路径开销**，而不是显著缩短一次性入口加载时间。

### 2.3 省电性与长期运行性能

没有 Perfetto / Batterystats 同设备长时间对照，因此不能直接给出“每版省电百分之几”的结论。但可以从代码层面给出**静态判断**：

| 版本 | 对省电/性能有利的改动 | 潜在风险 |
|---|---|---|
| r14.1.3 | 移除联网权限、支持资源、受限图标缓存 | 少量静态清理 |
| r14.2.0 | 偏好键一次规范化；清除无效参数复制；秒针改单调度 | 改动面广，需完整验证 |
| r14.2.4 | 热路径缓存、反射哨兵、无效 Hook/Receiver 防重注册 | 反射缓存需避免过期 |
| r14.2.7 | 自定义动作 gate，未配置时不注册 Controller/Receiver | 功能关闭时真正零成本 |
| r14.2.9 | BatteryIndicator 缓存 `mDensity`/`mStatusBarHeight`、复用 `Matrix` | 状态栏刷新每帧减少对象分配 |
| r14.3.1 | `WeatherDataController` 用单一 `ExecutorService` 替代每分钟 `new Thread()` | 线程池需正确关闭 |
| r14.5.0 | `getResId()` 缓存，`Handler(Looper.getMainLooper())` 统一，`RECEIVER_*` 生命周期 | 稳定基准 |
| r14.6.0 | 通知渠道/权限声明本身无性能收益；Settings.System → SharedPreferences 导致崩溃 | **负收益** |
| r14.6.1 | 回退崩溃源，保留通知渠道/权限 | 与 r14.5.0 接近，但多 13 KB 未使用代码/权限 |

**静态结论**：
- **r14.2.7 及以后**在“功能未开启时不做无用功”方面做得更好，有助于降低待机唤醒与 CPU 占用。
- **r14.2.9 / r14.3.1**在状态栏刷新、天气查询等高频路径上减少了对象分配和线程创建，对省电与流畅度有正面作用。
- **r14.6.x**并没有带来性能提升，只是 D 类 API 迁移尝试；迁移失败反而造成稳定性回退。

### 2.4 架构质量

架构演进整体是正向的：

1. **API 101 混合架构**：r14.0.0 建立，r14.1.3 稳定。
2. **有界线程池 + Handler 归一**：r14.1.3 / r14.2.x 减少野线程。
3. **资源 ID 缓存 `getResId()`**：r14.5.0 把高频 `Resources.getIdentifier()` 收敛到线程安全缓存。
4. **生命周期 gate**：r14.2.7 / r14.3.1 开始“按开关决定是否注册 Hook/Receiver/Observer”，避免功能关闭时代码仍被执行。
5. **包名与工程结构统一**：r14.5.0 完成全量迁移，减少未来冲突。

**但 r14.6.0 是架构回退点**：试图用 `SharedPreferences` 替代 `Settings.System` 自定义键，没有充分评估 LSPosed 早期 Context 限制，导致 `systemui` 进程入口崩溃。r14.6.1 通过回退保住了稳定性，但也说明 **“新架构”不一定优于“经过验证的旧架构”**。

### 2.5 稳定性

| 版本 | 验证状态 | 结果 |
|---|---|---|
| r14.1.3 | 日志验证 | 无模块相关崩溃/ANR |
| r14.2.0 | 日志验证 | 无异常 |
| r14.2.4 / 2.7 / 2.9 | 日志验证 | 无异常 |
| r14.3.1 | 日志验证 | 无异常 |
| r14.5.0 | **完整重启验证** | **推荐稳定版** |
| r14.6.0 | 实机反馈 | statusbar/control center 重启后失效，LSPosed 出现 `RuntimeException: No data directory found for package android` |
| r14.6.1 | LSPosed 日志 | 无模块报错；功能回归待补充 |

**关键点**：
- r14.2.5 / r14.2.6 因状态栏过渡尝试导致双排信号图标深浅色切换异常，已删除 tag/release。
- r14.6.0 是“候选版即翻车”的案例：构建通过，但运行时 Context 假设错误。
- **目前最稳**：r14.5.0；**功能最新且仍稳定**：r14.3.1。

---

## 3. 核心结论

1. **版本号越大 ≠ 全面更优**。r14.6.0 比 r14.5.0 新，但引入了 systemui 启动崩溃；r14.6.1 只是热修复，没有新增性能收益。
2. **性能收益集中在 r14.1.3 → r14.3.1**：包括资源缓存、线程池化、高频 Hook 参数与反射治理、功能关闭零成本等。
3. **r14.5.0 是稳定性与架构的当前最佳平衡点**：包名迁移完成，`getResId()` 缓存落地，生命周期加固，且经过完整重启验证。
4. **r14.6.1 目前日志干净，但仍是候选版**：需要完整重启后确认 statusbar/control center 功能正常，再决定是否提升为稳定版。
5. **省电性无法从 APK 大小或加载时间推导**：需要同设备、同功能开关、同使用场景下的 Perfetto / Batterystats 长时间采样。静态分析只能说 r14.2.7 / r14.3.1 在减少无效唤醒与对象分配方面更优。

---

## 4. 建议

- **日常推荐**：继续以 **r14.5.0** 为稳定基线。
- **候选验证**：r14.6.1 日志已无模块报错，接下来应在真实设备上完整重启，逐项验证 statusbar / control center / 锁屏 / 天气 / 电池指示器等核心功能。
- **后续版本策略**：
  - 优先把 r14.6.1 的“通知渠道 + 权限声明”逐步稳定化，而不是继续追加新的 D 类迁移。
  - 若要做 Settings.System 自定义键迁移，应改为在 `Application.onCreate` 或 `ContentProvider` 就绪后再读写，避免 `onPackageReady` 早期 Context 不可用。
  - 避免“体积增加 = 功能增强”的错觉，r14.6.0 增加了 13 KB 并造成崩溃，是负向案例。


---

## 附录：r14.6.1 与 r14.5.0 全面对比

> 对比基准：
> - r14.5.0 最终代码：`d26c5c0`（包名迁移完成并标记为稳定版）。
> - r14.6.1 最终代码：`81a2e38`（回退 r14.6.0 崩溃源后的候选版）。
> 注：GitHub 远端标签 `r14.5.0`/`r14.6.0`/`r14.6.1` 当前均指向同一旧提交 `797f9d5`（r14.4.0），与本仓库最新 `main` 不同步；以下对比以实际代码提交为准。

### 1. 代码差异统计

`git diff d26c5c0..81a2e38 --stat`：

```
 CHANGELOG.md                                       | 42 +-----
 README.md                                          |  8 +++-
 app/build.gradle                                   |  4 +--
 app/src/main/AndroidManifest.xml                   |  2 +
 .../customiuizer/MainApplication.java              | 12 ++++++++++++
 .../customiuizer/utils/Helpers.java                | 14 ++++++++++++++
 6 files changed, 76 insertions(+), 6 deletions(-)
```

**结论**：r14.6.1 相对 r14.5.0 只新增了 6 个文件的少量改动，没有业务逻辑重构，也没有新增用户可见功能。

### 2. 功能差异

| 维度 | r14.5.0 | r14.6.1 | 影响 |
|---|---|---|---|
| 状态栏/控制中心 Hook | 完整可用 | 完整可用（r14.6.0 曾崩溃，r14.6.1 已回退修复源） | r14.6.1 修复了 r14.6.0 的 systemui 启动崩溃 |
| 包名 | `tv.withaibuild.customiuizer.r14` | 相同 | 一致 |
| 自定义键存储 | 使用 `Settings.System`（`systemui_restart_time`、`last_music_paused_time`、`dark_mode_enable_by_setting`） | **回退为 `Settings.System`**；r14.6.0 尝试改 `SharedPreferences` 失败 | r14.6.1 与 r14.5.0 在此完全一致 |
| 通知渠道 | 无 | `MainApplication.onCreate` 创建默认 `NotificationChannel`（ID `customiuizer_default`，低重要性） | 仅在应用进程启动时创建一次，对 SystemUI/系统框架无直接影响 |
| 权限声明 | 无 `WAKE_LOCK`、`POST_NOTIFICATIONS` | 新增 `WAKE_LOCK` 和 `POST_NOTIFICATIONS` 权限 | 目前模块源码未实际使用这两个权限，仅声明；无运行时行为变化 |
| PendingIntent 辅助 | 无 | `Helpers` 新增 `getMutableActivityPendingIntent` / `getImmutableActivityPendingIntent` | 源码当前未调用这两个方法；为后续新增 PendingIntent 时统一 flag |

**功能实质**：r14.6.1 没有新增用户可见功能；它是 r14.6.0 的“撤销补丁”加上两项未来兼容性铺垫（通知渠道、权限声明、PendingIntent 辅助）。

### 3. 性能与稳定性

| 指标 | r14.5.0 | r14.6.1 | 说明 |
|---|---|---|---|
| APK 大小 | 2,886,373 B | 2,900,233 B | +13,860 B（约 +13.9 KB），主要来自通知渠道类、权限字符串、PendingIntent 辅助方法，以及 R8 重排后的代码布局变化 |
| 模块加载时间 | 实测未在 README 中单列，约在 8–15 ms 区间 | 待补充 | r14.6.1 入口未增加运行时逻辑，预计与 r14.5.0 相当 |
| 高频 Hook 开销 | 已收敛到 `getResId()` 缓存、gate 控制、线程池化 | 与 r14.5.0 相同 | r14.6.1 未触碰 `GlobalActions`、`SystemUI`、`System` 等核心热路径 |
| 运行时对象分配 | 已优化 | 与 r14.5.0 相同 | 通知渠道只创建一次；PendingIntent 辅助未使用 |
| 系统启动崩溃 | 无 | r14.6.0 曾崩溃；r14.6.1 已修复 | r14.6.1 日志中已无 `No data directory found for package android` |
| 完整重启验证 | README 记录为已通过 | 待补充 | r14.6.1 目前 LSPosed 日志无模块报错，但用户功能回归测试尚未完成 |
| 稳定版标识 | 是 | 候选版 | r14.5.0 是当前推荐稳定基线 |

### 4. 架构质量

| 维度 | r14.5.0 | r14.6.1 | 评价 |
|---|---|---|---|
| 包名/工程结构 | 已全部迁移到 `tv.withaibuild.customiuizer` | 未再改动 | r14.5.0 完成架构迁移，r14.6.1 继承 |
| 生命周期治理 | `RECEIVER_EXPORTED`/`RECEIVER_NOT_EXPORTED` 已补齐，`Handler(Looper.getMainLooper())` 已统一 | 未再改动 | 一致 |
| 资源缓存 | `Helpers.getResId()` 热路径缓存已落地 | 未再改动 | 一致 |
| 线程模型 | 有界线程池、单一 `ExecutorService`、`TIME_TICK` 接收者防重注册 | 未再改动 | 一致 |
| API 兼容性 | 保持 `libxposed API 101`，目标 TIRAMISU（API 33） | 未变动 | 一致 |
| 代码冗余 | 无 `PendingIntent`/`NotificationChannel` 辅助 | 新增未使用的 `PendingIntent` 辅助与未使用的 `SharedPreferences` import | 轻微增加死代码与导入冗余；不影响运行 |

### 5. 安全与权限面

| 权限 | r14.5.0 | r14.6.1 | 实际使用 |
|---|---|---|---|---|
| `WAKE_LOCK` | 无 | 声明 | 模块源码当前未获取 `WakeLock` |
| `POST_NOTIFICATIONS` | 无 | 声明 | 通知渠道已创建，但模块源码未主动发送通知 |

**影响**：仅在 `AndroidManifest.xml` 中声明权限，不会自动产生后台行为；但安装时会向用户展示新增权限，对上架/用户信任有轻微影响。

### 6. 版本控制/发布面（注意）

- **远端标签不同步**：GitHub 上 `r14.5.0`/`r14.6.0`/`r14.6.1` 三个 tag 当前都指向提交 `797f9d5`（r14.4.0 规划版），而最新 `main` 已推进到 `81a2e38`。如果用户从 Release 页下载 Source code 会拿到旧代码，只有 APK 资产是最新的。
- **签名与包名一致**：r14.6.1 使用与 r14.5.0 相同的签名证书和 `applicationId`，可以覆盖安装。

### 7. 综合结论

**r14.5.0 仍然是当前最稳、最轻量的推荐版本。**

- r14.6.1 没有带来任何性能、省电或架构上的净提升。
- r14.6.1 只是修复了 r14.6.0 自己引入的崩溃，并提前声明了未来可能用到的通知/权限/PendingIntent 基础设施。
- 从 r14.5.0 升级到 r14.6.1 的收益：消除 r14.6.0 的 systemui 启动崩溃风险（但 r14.5.0 本身没有这个问题），获得未来通知渠道的兼容性。
- 从 r14.5.0 升级到 r14.6.1 的成本：APK 增大 13.9 KB、多出两个权限声明、候选版未做完整功能回归验证。

**建议**：
- 如果只求稳定省电，继续留在 **r14.5.0**。
- 如果已经在用 r14.6.0 并且遇到 statusbar/control center 重启失效，应升级到 **r14.6.1**，但需完整重启后逐项验证 statusbar / control center / 锁屏 / 天气 / 电池指示器等功能。
- r14.6.1 通过完整验证后，才考虑把它提升为新的稳定版；在此之前不要因为它版本号更高就认为“全面更优”。
